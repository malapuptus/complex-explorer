# Verify workflow — forwards to CI.
# Canonical CI is in .github/workflows/ci.yml.
# This file is kept to avoid breaking existing branch protections.
name: Verify

on:
  push:
  pull_request:

jobs:
  verify:
    uses: ./.github/workflows/ci.yml
